const btnOvelay = (section, image, btn){
const button =  document.quarySelector(btn),
text =  document.quarySelector(section),
cow =  document.quarySelector(image);

button.addEventListener('click', ()=>{
console.log('ggg')

});


}